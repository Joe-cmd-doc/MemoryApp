<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:gravity="center"
    android:background="#274B34"
    android:padding="16dp">

    <!-- Mensaje de Nivel Completado centrado -->
    <TextView
        android:id="@+id/textViewNivelCompletado"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Enhorabona! Has completat el nivell."
        android:textSize="20sp"
        android:textColor="#FFFFFF"
        android:textStyle="bold"/>

    <!-- Botón para siguiente nivel centrado -->
    <Button
        android:id="@+id/buttonSeguentNivell"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Següent nivell"
        android:textSize="18sp"
        android:textColor="#FFFFFF"
        android:background="#00796B"
        android:layout_marginTop="30dp"/>

    <!-- Botón para selección de niveles centrado -->
    <Button
        android:id="@+id/buttonSeleccioNivells"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Anar a selecció de nivells"
        android:textSize="18sp"
        android:textColor="#FFFFFF"
        android:background="#00796B"
        android:layout_marginTop="20dp"/>

    <!-- Botón para el menú principal centrado -->
    <Button
        android:id="@+id/buttonMenuPrincipal"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Menú principal"
        android:textSize="18sp"
        android:textColor="#FFFFFF"
        android:background="#00796B"
        android:layout_marginTop="20dp"/>

</LinearLayout>
